{
  "license": "MIT",
  "directories": {
    "lib": "src",
    "test": "tests"
  },
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  },
  "scripts": {
    "test": "tsc && jest",
    "coverage": "tsc && jest --coverage",
    "test:watch": "jest --watchAll"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "jest-mock-extended": "^3.0.4",
    "ts-jest": "^29.0.5"
  }
}
